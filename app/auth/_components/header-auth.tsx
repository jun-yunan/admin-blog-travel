import React from 'react';

type Props = {};

const HeaderAuth = (props: Props) => {
  return (
    <div className="w-full h-[60px] bg-purple-300 flex items-center justify-center">
      HeaderAuth
    </div>
  );
};

export default HeaderAuth;
